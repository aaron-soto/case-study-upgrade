import { Event } from "@/types/Events";
import EventsList from "@/components/sections/Anew/EventsList";
import React from "react";

const EventsListWrapper = async () => {
  return <EventsList />;
};

export default EventsListWrapper;
